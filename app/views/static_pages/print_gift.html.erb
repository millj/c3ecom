<% provide(:title, 'Print Gift Messages') %>
<h2>Print Gift Messages</h2>


<%= form_tag(controller: 'static_pages', action: 'print_gift_message', method: "post") do %>
    <!-- Form Name -->
    <legend>Select your message to print!</legend>
    This is a list of orders with gift messages current </p>
    <% unless @gift_message.nil? %>
        <table class="table table-hover table-condensed">
          <tr>
            <th><p class="text-left">Selected</p></th>
            <th><p class="text-centre"> Order #</p></th>
            <th><p class="text-centre"> Message #</p></th>
          </tr>
          <% @gift_message.each do |message| %>
              <tr>
                <td><%= check_box_tag 'selected[]', message['order_number'], checked = true %></td>
                <td><%= message['order_number'] %></td>
                <td><%= message['gift_message'] %></td>
              </tr>
          <% end %>
        </table>
    <% end %>

    </p> </p>
    <div>
       <%= submit_tag 'Print Messages!', class: 'btn btn-warning btn-large' %>
    </div>

<% end %>


