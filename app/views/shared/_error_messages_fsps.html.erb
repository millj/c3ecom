<% if @fct_store_pick_scans.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-error">
        The form contains <%= pluralize(@fct_store_pick_scans.errors.count, "error") %>.
      </div>
      <ul>
        <% @fct_store_pick_scans.errors.full_messages.each do |msg| %>
            <li>* <%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>