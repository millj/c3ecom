<% if @direct_bin_contents.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-error">
        The form contains <%= pluralize(@direct_bin_contents.errors.count, "error") %>.
      </div>
      <ul>
        <% @direct_bin_contents.errors.full_messages.each do |msg| %>
            <li>* <%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>