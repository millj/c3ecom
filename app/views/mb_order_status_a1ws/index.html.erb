<% provide(:title, 'CFC A1w Orders') %>
<h1>CFC A1w Orders</h1>


<form class="form-horizontal">
  <fieldset>

    <!-- Form Name -->
    <legend>Filters</legend>

    <!-- Search input-->
    <div class="control-group">
      <label class="control-label" for="search"> Order #</label>
      <div class="controls">
        <input id="search" name="search" class="input-medium search-query" type="text" autofocus="true">
      </div>
    </div>

  </fieldset>
</form>

<%= will_paginate %>

<ul class="users">
  <table class="table table-striped">
    <tr>
      <th> Order # </th>
      <th> Web Order GUID </th>
      <th> Priority </th>
      <th> Order Status </th>
      <th> Connote # </th>
      <th> RPRO Invoice #</th>
    </tr>

  <% @mb_order_status_a1ws.each do |mb_order| %>
      <tr>
        <td><%= mb_order.order_number %></td>
        <td><%= mb_order.order_guid %></td>
        <td><%= mb_order.priority %></td>
        <td><%= mb_order.order_ecom_status %></td>
        <td><%= mb_order.connote_reference %></td>
        <td><%= mb_order.created_rpro_invoice_no %></td>
      </tr>
  <% end %>
  </table>
</ul>

<%= will_paginate %>